@model WebApp.Models.Operador

@{
    ViewData["Title"] = "Editar Operador";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2>Editar Operador</h2>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" method="post">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                        
                        <input type="hidden" asp-for="Id" />
                        
                        <div class="mb-3">
                            <label asp-for="Nombre" class="form-label"></label>
                            <input asp-for="Nombre" class="form-control" />
                            <span asp-validation-for="Nombre" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Apellido" class="form-label"></label>
                            <input asp-for="Apellido" class="form-control" />
                            <span asp-validation-for="Apellido" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="NumeroEmpleado" class="form-label"></label>
                            <input asp-for="NumeroEmpleado" class="form-control" />
                            <span asp-validation-for="NumeroEmpleado" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label for="CodigoPin" class="form-label">Nuevo Código PIN (opcional)</label>
                            <input type="password" id="CodigoPin" name="CodigoPin" class="form-control" 
                                   placeholder="Dejar en blanco para mantener el PIN actual" 
                                   pattern="[0-9]*" 
                                   inputmode="numeric" />
                            <div class="form-text">Solo ingrese un nuevo PIN si desea cambiarlo. Debe tener al menos 4 dígitos.</div>
                        </div>

                        <div class="mb-3 form-check">
                            <input asp-for="Activo" class="form-check-input" />
                            <label asp-for="Activo" class="form-check-label"></label>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Departamentos</label>
                            @if (ViewBag.DepartamentosOperador != null && ((List<WebApp.Models.DepartamentoOperador>)ViewBag.DepartamentosOperador).Any())
                            {
                                var departamentosSeleccionados = (List<int>)ViewBag.DepartamentosSeleccionados;
                                <div class="border rounded p-3">
                                    @foreach (var depto in (List<WebApp.Models.DepartamentoOperador>)ViewBag.DepartamentosOperador)
                                    {
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox" name="DepartamentosSeleccionados" value="@depto.Id" id="depto_@depto.Id"
                                                   @(departamentosSeleccionados.Contains(depto.Id) ? "checked" : "")>
                                            <label class="form-check-label" for="depto_@depto.Id">
                                                @depto.Nombre
                                                @if (!string.IsNullOrEmpty(depto.Descripcion))
                                                {
                                                    <small class="text-muted">- @depto.Descripcion</small>
                                                }
                                            </label>
                                        </div>
                                    }
                                </div>
                            }
                            else
                            {
                                <div class="alert alert-warning">
                                    No hay departamentos disponibles. <a asp-controller="DepartamentosOperador" asp-action="Create">Crear un departamento</a>
                                </div>
                            }
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Cancelar
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save"></i> Actualizar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}